"""SplatApp v2 worker — COLMAP (primary) / DUSt3R (fallback) pose estimation
feeding a LichtFeld Studio MRNF training run, behind the same HTTP + queue
contract as the original ``worker/`` service.
"""
