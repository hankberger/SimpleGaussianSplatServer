# Core worker deps for the Docker image — the COLMAP → LichtFeld → cleanup →
# convert happy path. No torch/gsplat/dust3r here: those are only for the DUSt3R
# fallback + the preview thumbnail, both optional/non-fatal. Add them in a second
# layer if you want them (see README), but they greatly inflate build time.
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic-settings>=2.1.0
httpx>=0.25.0
numpy>=1.24.0
scipy>=1.11.0
opencv-python-headless>=4.8.0
plyfile>=1.0
pycolmap>=0.6.1
